/* Convert raw bytes into a hexidecimal representation (twice the length) */
unsigned char *bytesToHex(unsigned char *bytes, int length);
/* Convert a hexadecimal representation into raw bytes (half the length) */
unsigned char *hexToBytes(unsigned char *bytes, int length);

